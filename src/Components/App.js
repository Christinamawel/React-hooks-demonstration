import Timer from './Timer';

function App() {

  const styles = {
    fontFamily: "Ariel",
    textAlign: "center"
  }

  return (
    <div style={styles}>
      <Timer />
    </div>
  );
}

export default App;
